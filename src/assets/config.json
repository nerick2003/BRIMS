{
  "apiBaseUrl": "https://YOUR_BACKEND_URL",
  "_hint": "Frontend is on Firebase Hosting; backend runs elsewhere. Deploy backend to Railway/Render (see backend/README.md), then set apiBaseUrl here to that URL (no trailing slash)."
}
